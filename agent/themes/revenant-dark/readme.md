# revenant-dark — PLACEHOLDER

Inversion of `revenant-light`. Same brand identity, dark surface. Does not exist yet — Phase 0.

## Authoring rule

Author `revenant-light` first (when inspo lands). `revenant-dark` is the inversion: invert surface + foreground + lines, keep `--accent` and type identity, adjust shadows to honest blacks.

See `agent/themes/lore-dark/colors_and_type.css` (when present) for the inversion pattern from `lore-light`.
